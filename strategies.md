# 边缘服务器调度策略全集

## 一、已实现策略（7 种）

### 1.1 Random — 随机分配

```
逻辑：候选服务器中等概率随机选一台
复杂度：O(1)
价格感知：否
容量感知：否（依赖 allocate_one 的 retry 兜底）
```

| 场景   | 表现            |
| ---- | ------------- |
| 任何场景 | 永远最差或次差，无优化意识 |

***

### 1.2 Round Robin — 轮询

```
逻辑：维护全局指针，依次循环分配
复杂度：O(M)
价格感知：否
容量感知：否
```

| 场景  | 表现                        |
| --- | ------------------------- |
| 无价差 | 优于 random，劣于 load_balance |
| 有价差 | 远劣于 cheapest              |

***

### 1.3 Load Balance — 负载均衡

```
逻辑：选当前时间片绝对负载最小的服务器，平局随机
复杂度：O(M)
价格感知：否
容量感知：是（选择本身不影响容量检测，但 retry 生效）
```

| 场景       | 表现                  |
| -------- | ------------------- |
| 无价差、容量不限 | 最优                  |
| 有价差      | 次优（差 cheapest 2-9%） |
| 全域平均排名   | 2.69（第 2）           |

***

### 1.4 Cheapest — 最便宜优先

```
逻辑：选单价最低的服务器，平局随机
复杂度：O(M)
价格感知：是
容量感知：否
```

| 场景            | 表现                   |
| ------------- | -------------------- |
| 有价差 + 容量充足    | 最优（胜率 61.5%）         |
| 无价差（价格全等）     | 退化为 random（最差）       |
| S1 容量极小（如 5k） | 频繁 retry，优势被压缩到 1-2% |

***

### 1.5 Power of Two — 双选比较

```
逻辑：随机挑 2 台，选负载更小的
复杂度：O(2)
价格感知：否
容量感知：轻微
理论保证：最大负载 ≈ O(log log M)
```

| 场景   | 表现                        |
| ---- | ------------------------- |
| 全域   | 始终中游，接近 load_balance，从未最优 |
| 平均排名 | 4.23                      |

***

### 1.6 Load Ratio — 负载百分比均衡

```
逻辑：选 load(s)/capacity(s) 最小的服务器
复杂度：O(M)
价格感知：否
容量感知：是（按比例）
```

| 场景   | 表现                         |
| ---- | -------------------------- |
| 容量均匀 | 等于 load_balance            |
| 容量递增 | 劣于 load_balance（+0.4-2.6%） |
| 容量递减 | 略优 load_balance（-1.8%）     |

**失败原因**：按容量比例保护小服务器，导致大服务器的 Q95 膨胀。Q95 丢弃 top 5% 的机制下，"让小服务器满到 100% 并利用其免费 5%"，优于"让小服务器空闲保护它"。

***

### 1.7 H3 Tiered — 分层混合（改进版）

**需要维护的状态**：无（分层边界在仿真开始时确定一次，后续不变）。

**核心改进**：Tier1 不再按服务器序号划分，而是**根据场景特征自动选择排序依据**。

**分层规则**：

```
═══ 仿真开始时确定分层（仅一次）═══

  if max(price) == min(price):        ← 价格一致 → 无价差场景
      排序依据 = capacity 升序        ← 小容量在前，大容量在后
      原因：无价差时，把负载集中到小容量服务器，让它们的免费 5% 被充分利用
            （小容量 → 填到 100% Q95 = 容量上限 → free 5% 生效 → ΣQ95 降低）
  else:                                ← 价格不一致 → 有价差场景
      排序依据 = price 升序            ← 低价格在前，高价格在后
      原因：有价差时，优先填满便宜服务器来降低加权成本

  按排序依据排列服务器 → 取前 ceil(M×0.6) 台 → 构成 Tier1
  剩余 → Tier2

═══ 每个请求到达时 ═══
  tier1_candidates = Tier1服务器 ∩ 容量足够的候选
  if tier1_candidates 非空:
      选 cheapest(tier1_candidates)     ← Tier1 内按价格选（同价时随机）
  else:
      选 load_balance(所有剩余候选)      ← Tier1 全满，Tier2 均衡接管

Tier1 策略 = cheapest（优先填满这些服务器）
Tier2 策略 = load_balance（溢出后均衡分配）
```

**三种场景下的分层效果**：

| 场景              | 示例                                   |     排序依据    | Tier1 包含                                | 效果                                            |
| --------------- | ------------------------------------ | :---------: | --------------------------------------- | --------------------------------------------- |
| 价格一致，容量不一致（A 组） | Cap=[5k,9k,18k,27k,36k], Price 全 1.0 | capacity 升序 | S1(5k), S2(9k), S3(18k)                 | 3 台小容量集中填满 → ΣQ95 ↓，比 ld_bal 优 0.1-0.2%       |
| 容量一致，价格不一致（B 组） | Cap 全 19k, Price=[1.0→1.5]           |   price 升序  | S1(1.0), S2(1.12), S3(1.25)             | 3 台低价集中填满 → 接近 cheapest                       |
| 都不一致（C 组）       | Cap=[5k→36k], Price=[1.0→1.3]        |   price 升序  | S1(5k,1.0), S2(9k,1.07), S3(18k,1.15)   | S1 小容量+低价 → 双重集中效果最大化                         |
| 都不一致（C3 递减）     | Cap=[36k→5k], Price=[1.0→1.3]        |   price 升序  | S1(36k,1.0), S2(27k,1.07), S3(18k,1.15) | S1 大容量+低价 → 等于 cheapest（单台吃满）                 |
| 容量无限（D1）        | Cap 全 950k, Price 全 1.0              | capacity 升序 | 任意 3 台（容量相同）                            | **仍差**：3 台分担 > 5 台均衡分担 → ΣQ95。需 Adaptive 方案解决 |

**与当前代码的区别**：当前代码（`simulation_engine.m:181`）用 `1:n_tier1` 按序号分，改进后用排序后的前 N 台分。

**复杂度**：O(M)。价格感知：是。容量感知：是（排序依据自动选择）。序号依赖：已消除。

| 场景         | 表现                                                                   |
| ---------- | -------------------------------------------------------------------- |
| 无价差 + 容量递增 | 最优（容量升序排序, 小容量进 Tier1）                                               |
| 有价差 + 容量充足 | 等于 cheapest（价格升序排序, 低价进 Tier1）                                       |
| 无价差 + 容量无限 | **最差场景**：容量全相同 → 任意 3 台进 Tier1 → 3×Q95 > 5×Q95（与旧版一致, 需 Adaptive 解决） |
| 全域预期排名     | ≤ 2.08（改进后无序号依赖, 预期排名持平或更优）                                          |

***

## 二、待实现策略（4 种）

### 2.1 OnTPC 简化版 — 免费槽感知

**需要维护的状态**（跨时间片持久）：

| 变量                                                 | 含义               | 更新时机          |
| -------------------------------------------------- | ---------------- | ------------- |
| `Q95_ref(s)`                                       | 服务器 s 的固定 Q95 阈值 | 预热结束后算一次，后续不变 |
| `burst_count(s)` | 当前周期中 H(s,t) > Q95_ref(s) 的累计次数 | 每个时间片结束时 +1 或不变  |               |
| `free_left(s)` | 15 - burst_count(s)，免费槽配额余额       | 每个时间片开始时刷新       |               |
| `L_remain(s)` | C_k - 当前时间片 H(s,t)，冲顶可用空间          | 每次分配后更新          |               |

**实现步骤**：

```
═══ 初始化 ═══
前 3 天（864 个时间片）：用 load_balance 跑，收集 H_pre(s,:)

第 3 天结束后：
  Q95_ref(s) = percentile95( H_pre(s,:) )   ← 固定，后续永远不变
  burst_count(s) = count( H_pre(s,:) > Q95_ref(s) )
  free_left(s) = 15 - burst_count(s)        ← 预热期已占用的也扣掉

═══ 每个时间片 t 开始时 ═══
  L_remain(s) = C_k - H(s,t)                ← 当前负载到容量还有多少空间
  // free_left(s) 已在上一个时间片结束时更新好

═══ 每个请求到达时 ═══
  （与 allocate_one 的 retry 循环配合）

  Step 1: 找已冲顶的
    bursted = { s: H(s,t) > Q95_ref(s) }
    if bursted 非空:
        ① 选 free_left 最多的     ← 配额均衡
        ② 平局选 L_remain 最大的  ← 避免小服务器先满
        ③ 再平局选 price 最低的   ← tiebreaker

  Step 2: 需要开启新冲顶
    candidates = { s: free_left(s) > 0 且 H(s,t) + demand ≤ C_k }
    if candidates 非空:
        选 price 最低的  ← 消耗它的一个免费配额

  Step 3: 兜底
    所有配额用尽或无容量
    选 price 最低的  ← 硬扛, 推高 Q95

═══ 每个时间片 t 结束时 ═══
  for s = 1..M:
      if H(s,t) > Q95_ref(s):  burst_count(s) += 1
  free_left(s) = 15 - burst_count(s)
```

**关键设计决策**：

* Q95_ref 用预热期数据算一次就固定——避免阈值漂移（见 OnTPC_ADAPTATION.md §2.2）

* 预热期占用的 burst_count 也要从 15 中扣除——第一天的 free_left 可能已经是 12 而非 15

* L_remain 实时更新（每次分配后减少），free_left 按时间片粒度更新（时间片结束时）

***

### 2.2 Price-Q95 Ratio — 连续评分（双信号模型）

**核心思想**：对每台候选服务器计算一个连续的"等效价格"评分，选评分最低的。评分公式为 `price × (1 + 负载惩罚)`。**不需要离散的分层或阈值**——随着负载上升，服务器的等效价格连续增长，请求自动"滑向"更便宜的服务器。

**需要维护的状态**：

| 变量              | 含义                   | 更新时机                    |
| --------------- | -------------------- | ----------------------- |
| `pq95_hist(s)`  | 服务器 s 的历史 Q95 利用率估计 | 每个时间片开始时重算一次（缓存复用全片请求） |
| `H(s, t)`       | 服务器 s 当前时间片的累计负载    | 每次分配后实时更新              |

**实际实现（双信号评分）**：

```
═══ 每个时间片 t 开始时 ═══
  if t > 1:
      pq95_hist(s) = Q95( H(s, 1:t-1) ) / capacity(s)   ← 历史利用率, 范围 [0,1]
  else:
      pq95_hist(s) = 0
  // 全时间片复用, 避免每个请求都排序 O(N_req × log N)

═══ 每个请求到达时 ═══
  for each candidate s:
      cur  = H(s, t) / capacity(s)                        ← 当前时间片负载率
      hist = pq95_hist(s)                                  ← 历史 Q95 利用率
      score(s) = price(s) × [ 1  +  α_load × cur  +  α_q95 × hist ]

      主信号        辅信号
      (当前负载压力)   (历史计费状态)

  α_load = 2.0    ← 当前负载惩罚权重 (主信号: 防止当前时间片溢出/冲顶)
  α_q95  = 0.5    ← 历史 Q95 惩罚权重 (辅信号: 引导长期 Q95 分布)

  选 score 最小的
```

**为什么需要两个独立参数（而非单 α）**

单 α 方案（`score = price × (1 + α × Q95_est/cap)`）把当前压力和历史状态压缩到一个维度，存在三个根本缺陷：

**缺陷 1：时间尺度冲突。** `cur` 和 `hist` 变化速度相差约 8000 倍。一个时间片只有 5 分钟——cur 可以在几秒内从 0 飙到 1.0。Q95 是 30 天 8640 个时间片的统计量——hist 每天才变动约 1%。用单个 α 同时驱动这两种响应，必然失衡：α 大了→过度反应 cur 的瞬时波动（请求在服务器间乒乓）；α 小了→hist 几乎不起作用（退化为 cheapest）。

**缺陷 2：早期估计不可靠。** 仿真第 1 天只有 288 个时间片样本。此时 `Q95_est` 噪声极大——一个偶然的高负载时间片就能把它推高，但策略无法区分"真高 Q95"和"样本不足导致的虚高"。单 α 下，这个不可靠的 hist 直接进入评分，误导决策。

**缺陷 3：无价差时无法打破对称性。** 当 `price(s) ≡ 1.0`，单 α 方案退化为 `score = 1 + α × hist`。对于容量相同的服务器，hist 在统计意义上趋同→所有 score 接近→随机选。α 再大也只是放大噪声，无法产生"故意集中到一台"的效果。双信号至少保留了 `cur` 信号在当前片内的区分力。

**双信号的物理直觉**：

```
α_load × cur    →  "别让当前片炸了"  (急, 秒级反应)
α_q95  × hist   →  "长期Q95分布别偏" (缓, 天级惯性)
```

| 维度 | cur 信号 (α_load) | hist 信号 (α_q95) |
|------|:---:|:---:|
| 时间尺度 | 5 分钟（单时间片） | 30 天（8640 时间片） |
| 变化速度 | 秒级（每次分配+demand） | 天级（每 288 片重算一次） |
| 物理含义 | 容量硬约束前置 —— 当前片满了就是满了 | Q95 计费结构 —— 历史已高的服务器"加量免费" |
| 失效模式 | α 太小→当前片被打爆→该片成 top 5% | α 太小→退化为 cheapest, 单台 Q95 被过度推高 |
| 过度模式 | α 太大→请求在服务器间乒乓, 均摊→ΣQ95 膨胀 | α 太大→过度回避历史高 Q95 服务器→浪费其免费 5% 配额 |

两参数独立调节的策略含义：**有价差时价格信号足够区分服务器，α_load 可以小一些（让价格主导）；无价差时 α_load 必须大（唯一区分手段）——但连续评分模型在无价差下有结构性退化，无法仅靠调参解决**（见下文实验结果）。

**设计直觉**：
- `α_load = 2.0 >> α_q95 = 0.5`：当前负载是主导信号。一个时间片只有 5 分钟，容量是硬约束——当前片满了就是满了，历史的"便宜"不能补偿当前片的溢出。
- `hist` 权重小但持续：它在每个请求中累积作用，引导请求在长时间尺度上偏向低 Q95 服务器。即使当前负载均等（各服务器 cur 相同），hist 信号也会推动差异化。

**评分示例（容量 19k，price 全 1.0）**：

| 服务器状态 | cur | hist | 1 + 2.0×cur + 0.5×hist | 等效价格 | 行为 |
|-----------|:---:|:----:|:----------------------:|------|------|
| S1, 当前片空, Q95=0 | 0 | 0 | 1.00 | ¥1.00 | 首选 |
| S1, 当前片 50%, Q95=0 | 0.5 | 0 | 2.00 | ¥2.00 | 已经被"惩罚" |
| S1, 当前片 90%, Q95=0 | 0.9 | 0 | 2.80 | ¥2.80 | **强烈排斥**（接近容量硬上限） |
| S1, 当前片空, Q95=100% | 0 | 1.0 | 1.50 | ¥1.50 | 即使当前空, 历史高 Q95 也有惩罚 |
| S1, 当前片 50%, Q95=100% | 0.5 | 1.0 | 2.50 | ¥2.50 | 双信号叠加, 强排斥 |
| S2, 当前片空, Q95=0 | 0 | 0 | 1.00 | ¥1.00 | S1 被排斥后自然转向 S2 |

当 S1 当前片的负载达到 50% 容量时，等效价格已涨到 2.0——比 S2 的初始 ¥1.00 贵一倍。请求自动流向 S2。**整个过程是连续的、无阈值的、无显式切换逻辑的。**

**缓存优化**：

`pq95_hist` 每个时间片只计算一次（对所有服务器），然后全时间片复用。因为一个时间片可能有数百个请求，如果每个请求都调用 `percentile95` 对所有服务器排序，复杂度是 `O(N_requests × M log T)`。缓存后降为 `O(M log T + N_requests × M)`，实际耗时约 4-15s（vs 未缓存版本的数十秒）。

***

#### 实施细节

**1. 引擎集成 — allocate_one 与策略的协作**

`simulation_engine.m` 中的分配流程（`allocate_one` 函数）与 price_q95 的交互：

```
每个请求到达时:
  ① 预过滤: candidates = find(H(:, t) < capacity)
     → 已满服务器不参与策略选择, 避免浪费"首选"在不可能的目标上
     → 若全部已满: 选负载最小的兜底, 不产生修正成本

  ② 策略评分: apply_strategy → strategy_price_q95
     → 对 candidates 中每台服务器计算 score
     → 返回 score 最小的服务器 c

  ③ 容量检查:
     if H(c, t) + demand ≤ capacity(c):  分配成功, 无惩罚
     else:  产生修正成本 = demand × price(c)
            从 candidates 移除 c, 回到 ② (此时 first_choice=false, 后续拒绝不重复计罚)

  ④ 全部候选都不够 → 选负载最小的兜底 (不丢包)
```

**关键设计**：预过滤（步骤①）与第 4 节描述的惩罚成本修复联动。旧代码中策略可能首选的服务器已经满载，导致修正成本被错误计入一个"明知不可用"的目标上。预过滤后，策略的首选始终是有剩余容量的服务器——修正成本才真正反映"策略想选但装不下"的意图。

**2. 历史 Q95 估计的计算时机**

```
每个时间片 t 开始:
  if t == 1:
      pq95_hist(:) = 0           ← 无历史, 纯 cur 决策
  else:
      for s = 1..M:
          pq95_hist(s) = percentile95( H(s, 1:t-1) ) / capacity(s)
      // 全时间片缓存, 该片内所有请求复用
      pq95_slot_done = t
```

`percentile95` 的计算复杂度是 `O(n log n)`（n = t-1，最大 8640）。缓存后每时间片只调用 M 次（M=5），而非每个请求都调用（每时间片约 70-80 次）。耗时从 ~50s 降至 ~5s（30 天仿真）。

**3. 参数默认值与调参空间**

| 参数 | 默认值 | 物理含义 | 扫参范围 |
|------|:---:|------|:---:|
| α_load | 2.0 | 当前负载惩罚 (主信号) | 0.5 ~ 5.0 |
| α_q95 | 0.5 | 历史 Q95 惩罚 (辅信号) | 0 ~ 2.0 |

**极值行为**：
- `α_load=0, α_q95=0` → score = price → **完全退化为 cheapest**
- `α_load→∞, α_q95=0` → 当前负载主导 → **退化为 load_balance**（永远选 cur 最小的）
- `α_load=0, α_q95→∞` → 历史 Q95 主导 → 过度回避高 Q95 服务器, 浪费其免费 5% 配额
- `α_load=2.0, α_q95=0.5` → **当前默认**, 有价差场景已为最优

**4. 与分配机制中修正成本的联动**

修正成本（penalty cost）的计算与策略无关——它由 `allocate_one` 统一处理。对 price_q95 的一个微妙影响：

```
场景: S1 (price=1.0) 当前负载率 0.95, S2 (price=1.07) 当前负载率 0.10
需求=500 bytes, S1 剩余容量=300, S2 剩余容量=2000

① 预过滤: S1 有剩余容量 (300>0), 参与候选
② 策略评分: cur(S1)=0.95, cur(S2)=0.10
   score(S1) = 1.0×(1+2.0×0.95+0.5×hist) ≈ 2.90
   score(S2) = 1.07×(1+2.0×0.10+0.5×hist) ≈ 1.28
   → 策略选 S2 (评分最低)
③ S2 容量 2000 ≥ 500 → 分配成功, 无惩罚 ✓
```

如果 S2 也装不下（假设 S2 剩余=200 < 500），则：
```
③ S2 拒绝 → penalty = 500 × 1.07 = 535 (S2的价格)
④ 回②, 重新评分 (candidates 已移除 S2) → 选 S1
⑤ S1 剩余=300 < 500 → 拒绝, 但 first_choice=false, 不重复计罚
⑥ ...继续或兜底
```

修正成本的单位是 `demand × price(首选被拒服务器)`，其物理含义是"策略最希望去的服务器装不下了，这个需求按它的单价来惩罚"。price_q95 的连续评分使得"首选"更精准（综合考虑了价格和负载），因此修正成本相对 cheapest（只看价格盲目堆）更低——这是一个自然的副作用。

***

#### 实验结果（15 个实验，α_load=2.0, α_q95=0.5 固定）

**有价差场景：全域最优**

| 实验 | 场景 | gap vs 最优 | 排名 |
|------|------|:---:|:---:|
| B1 | 均匀容量, 微价差[1→1.10] | 0.0% | **#1** |
| B2 | 均匀容量, 小价差[1→1.30] | 0.0% | **#1** |
| B3 | 均匀容量, 中价差[1→1.50] | 0.0% | **#1** |
| C1 | 递增容量[10k→30k], 低价=小容量 | 0.0% | **#1** |
| C2 | 递增容量[5k→36k], 低价=小容量 | 0.0% | **#1** |
| C3 | 递减容量[36k→5k], 低价=大容量 | 0.0% | **#1** |
| C4 | 递减容量[30k→10k], 低价=大容量 | 0.0% | **#1** |
| D2 | 满配[950k×5], 小价差 | 0.0% | **#1** |

在有价差的 8 个实验中，price_q95 **全部排名第一**。Q95 分布与 cheapest 一致（低价服务器满载，高价服务器趋近于 0），且在某些场景下 ΣQ95 略优于 cheapest（因为 hist 信号微调了满载服务器之间的配额分配）。

**无价差场景：严重退化**

| 实验 | 场景 | gap vs 最优 | 排名 |
|------|------|:---:|:---:|
| A1 | 均匀[19k×5], 无价差 | **+49.8%** | #9 (最差) |
| A2 | 温和递增[10k→30k], 无价差 | **+34.8%** | #9 (最差) |
| A3 | 中等递增[5k→36k], 无价差 | +14.0% | #9 (最差) |
| A4 | 陡峭递增[3k→41k], 无价差 | +11.6% | #9 (最差) |
| D1 | 满配[950k×5], 无价差 | **+90.1%** | #9 (最差) |

**退化原因分析**：

当所有服务器价格相同（`price(s) ≡ 1.0`）时，评分退化为：

```
score(s) = 1.0 × (1 + 2.0×cur + 0.5×hist)
         = 1 + 2.0×cur + 0.5×hist    ← 价格信号消失
```

cur 信号（α_load=2.0）主导决策：策略退化为"选当前负载最小的服务器"——即 **load_balance**。但 load_balance 在 Q95 计费下并非最优：Q95 的"丢弃 top 5%"机制奖励**集中**（让少数服务器满载，其余闲置），而 load_balance 追求**均衡**，导致所有服务器的 Q95 都处于中高水平。

具体观察 D1（无限容量+无价差，gap=90.1%）：
```
pq_ratio Q95 per server: [24262, 23491, 24036, 24208, 23654] → ΣQ95 = 119,650
ld_bal   Q95 per server: [12577, 12599, 12610, 12584, 12582] → ΣQ95 =  62,952
```
price_q95 把 5 台服务器全部填到 ~24k，但 load_balance 均衡后各约 12.6k——price_q95 因 cur 信号始终在"均衡"，导致 5 台 Q95 全高，ΣQ95 为 load_balance 的 **1.9 倍**。而 cheapest/h3_tiered 会集中到少数（3台~19k, 2台~3k）, ΣQ95 ≈ 63k。

**核心矛盾**：连续评分模型在有价差时天然契合——价格梯度提供了天然的"优先级排序"，cur+hist 信号在此基础上做负载微调。但无价差时，所有服务器的 base score 相同，cur 信号迫使策略在各服务器之间"轮流均摊"负载，恰好与 Q95 计费鼓励的"集中"背道而驰。**这是连续评分范式的结构性局限**——它只能做"惩罚高的少选"，不能做"故意选一台集中堆满"。

**与离散策略的本质区别**：

| 维度 | Price-Q95 Ratio | h3_tiered | OnTPC |
|------|:---:|:---:|:---:|
| 决策类型 | 连续评分 (双信号: cur+hist) | 离散分层 (Tier1/2) | 离散状态 (已冲顶/未冲顶) |
| 参数数量 | **2** (α_load, α_q95) | 1 (Tier1比例=0.6) | 0 (无自由参数) |
| 无价差行为 | cur 主导 → load_balance (**结构性退化, 不可调参修复**) | 容量升序排序 → 小容量进 Tier1 集中 | 预热 Q95_ref → 配额感知集中 |
| 有价差行为 | 价格主导 → 等价 cheapest + 微调 (全域最优) | 价格升序排序 → 等价 cheapest | 配额均衡消耗 → 最优 |
| Q95 感知方式 | **软感知**: hist 信号连续惩罚 | 无直接感知 | **硬感知**: burst_count 精确计数 |
| 容量感知方式 | **软感知**: cur 信号当前片实时压力 | 分层内生效 | L_remain 冲顶空间 |
| 优势 | 极度简单、无状态机、纯乘法、双信号解耦 | 自适应排序、全域稳健 | Q95 结构感知、免费槽配额 |
| 劣势 | 无价差退化、需扫参定 α | D1 退化（Tier1 永不满） | 需预热期、需 Q95_ref |

**改进方向**（待探索）：

1. **自适应 α(price_ratio)**：根据价格比动态调整 α_load 和 α_q95。无价差时增大 α 或引入非线性项（如 `cur²`）来产生集中效果。
2. **非线性评分**：当前是 `price × (1 + linear_penalty)`——线性惩罚在 price 全等时无法打破对称性。引入 `cur²` 或 sigmoid 饱和项：`score = price × (1 + α × cur²)`，让高负载的惩罚非线性加速，打破"轮流均摊"。
3. **引入容量偏好项**：`score(s) = price(s) × (1 + α_load×cur + α_q95×hist) × capacity_bias(s)`，其中 `capacity_bias` 在无价差时偏向小容量（利用其免费 5%），有价差时趋向 1.0。
4. **混合 price_q95 + tiered**：用 price_q95 的连续评分替代 h3_tiered 的离散 Tier1/2 边界，但保留"无价差时按容量排序"的预处理逻辑。

***

***

### 2.4 Adaptive Two-Tier — 自适应分层

**需要维护的状态**：

| 变量                                               | 含义           | 更新时机      |
| ------------------------------------------------ | ------------ | --------- |
| `tier1_size`                                     | Tier1 的服务器数量 | 每天结束时检查调整 |
| `tier1_set` | Tier1 包含哪些服务器（按价格排序前 tier1_size 台） | 调整时重新计算      |           |

**实现步骤**：

```
═══ 初始化 ═══
  tier1_size = max(1, ceil(M * 0.5))   ← 从 50% 开始 (比 H3 的 60% 保守)
  tier1_set = 按 price 升序前 tier1_size 台

═══ 每个请求到达时 ═══
  tier1_candidates = tier1_set ∩ {容量足够的服务器}
  if tier1_candidates 非空:
      选 cheapest(tier1_candidates)
  else:
      选 load_balance(所有剩余候选)

═══ 每天结束时（每 288 个时间片）═══
  计算 Tier1 的平均 Q95 利用率：
    avg_util = mean( Q95_est(s) / capacity(s) for s in tier1_set )

  if avg_util > 0.90：  ← Tier1 太满
      if tier1_size < M：
          tier1_size += 1
          tier1_set = 按 price 升序前 tier1_size 台
  elif avg_util < 0.50：  ← Tier1 太空
      if tier1_size > 1：
          tier1_size -= 1
          tier1_set = 按 price 升序前 tier1_size 台
```

**与 H3 的关键区别**：

* H3 按**序号**分 Tier1/2（`1:ceil(M*0.6)`），假设 S1-S3 永远是低价。服务器顺序变了就失效。

* Adaptive 按**价格**分 Tier1/2——价格最低的 N 台在 Tier1。始终正确。

* 大小自适应：D1（无限容量+无价差）下 Tier1 会慢慢缩到 1 台，然后因为 avg_util 低，切到 load_balance 行为。D2（无限容量+有价差）下 Tier1 自然包含 S1，且 S1 容量充足 → avg_util 不会触发调整 → 等于 cheapest。

***

### 2.5 Demand-Aware Tiered — 需求感知阶梯分配

**核心洞见**：请求大小是关键信息，现有策略全都没用上。一个小请求（50 bytes）塞给贵服务器影响极小；一个大请求（1400 bytes）塞给贵服务器代价极大。**先按请求大小分类，再决定分配逻辑。**

**阶段 1 — 请求分类**：

```
大请求: demand > min(capacity) × 5%   (如 > 250 bytes)
小请求: 否则
```

**阶段 2 — 分配决策**：

```
═══ 请求 d_k 到达 ═══

if d_k 是小请求:
    → 选负载率最小的服务器（load_balance）
      理由: 小请求无论去哪，对成本和 Q95 的影响都微乎其微，
      均衡分配能"填补"各服务器的零碎空间

if d_k 是大请求:
    → candidates = 容量足够的服务器
    → score(s) = price(s) / (1 - cur_ratio(s))
                  ↑                     ↑
            谁便宜选谁         当前片剩余空间越少惩罚越重
    → 选 score 最小的
═══════════════════════════════════════════════
```

**复杂度**：O(M)。价格感知：是。容量感知：是。历史依赖：无。

***

### 2.6 Demand-Aware Nonlinear Scoring — 需求感知非线性评分

**核心公式**：

$$\text{score}(s) = \frac{\text{price}(s)}{1 - \gamma_k \cdot \text{cur\_ratio}(s)}, \qquad \gamma_k = \frac{d_k}{\text{capacity}(s)}$$

其中 d_k 是当前请求大小，cur_ratio(s) = H(s,t) / capacity(s)。

**分配流程**：

```
═══ 每个请求 d_k 到达 ═══

  ① 预过滤: 剔除已满服务器
     if 全满 → 选负载最小的兜底

  ② 评分:
     for each candidate s:
         γ = d_k / capacity(s)
         cur_ratio = H(s,t) / capacity(s)
         score(s) = price(s) / (1 - γ × cur_ratio)
     选 score 最小的 c

  ③ 容量检查:
     if H(c,t) + d_k ≤ capacity(c) → 分配成功
     else → 修正成本 += d_k × price(c), 移除 c, 回到 ②
═══ ═══════════════════════════════════
```

**策略含义**：

| 要素 | 作用 | 物理直觉 |
|------|------|------|
| price(s) 在分子 | 基线排序 | 便宜的优先 |
| (1 - γ×cur) 在分母 | 非线性容量惩罚 | cur 接近 1/γ 时 score 暴增，强制换人 |
| γ = d_k/capacity(s) | 请求大小编码 | 大请求对小服务器 γ 更大，惩罚触发更早 |

**算例**（d_k=600 bytes）：

| 服务器 | cap | price | H(s,t) | cur | γ=600/cap | 1-γ×cur | score |
|------|------|------|------|------|------|------|------|
| S1 | 5000 | 1.00 | 4000 | 0.80 | 0.120 | 0.904 | 1.11 |
| S2 | 9000 | 1.05 | 6000 | 0.67 | 0.067 | 0.955 | **1.10** |
| S3 | 18000 | 1.10 | 2000 | 0.11 | 0.033 | 0.996 | 1.10 |

score 接近，S2 略低 → 600 bytes 分给 S2。

**极端场景**（S1 cur=0.95, d_k=1000 bytes）：

```
S1: γ=0.20, 1-γ×cur=0.81,  score=1.00/0.81=1.23
S2: γ=0.11, cur=0.1, 1-0.011=0.989, score=1.05/0.989=1.06  ← 胜出
```

非线性自动刹停：cur 从 0.8→0.95，score 从 1.11 跳至 1.23。

**核心特征**：零参数、零历史、O(M)。price 在分子区分方向，γ 把请求大小自动编码进惩罚灵敏度，非线性除法替代了手动调参。

| 特性 | 值 |
|------|:---:|
| 价格感知 | ✓ |
| Q95 感知 | ✗（无历史依赖） |
| 容量感知 | ✓（cur 实时 + 非线性刹车） |
| 请求感知 | ✓（γ 自动编码请求大小） |
| 参数数量 | **0** |
| 时间复杂度 | **O(M)** |

***

## 三、全域对比矩阵

| 策略                 | 价格感知 | Q95感知 |  容量感知  | 序号依赖 | 实现复杂度 |   平均排名   |
| ------------------ | :--: | :---: | :----: | :--: | :---: | :------: |
| random             |   ✗  |   ✗   |    ✗   |   ✗  |  O(1) |   6.62   |
| round_robin        |   ✗  |   ✗   |    ✗   |   ✗  |  O(M) |   5.54   |
| load_balance       |   ✗  |   ✗   |    ✓   |   ✗  |  O(M) |   2.69   |
| cheapest           |   ✓  |   ✗   |    ✗   |   ✗  |  O(M) |   3.31   |
| power_of_two       |   ✗  |   ✗   |   轻微   |   ✗  |  O(2) |   4.23   |
| load_ratio         |   ✗  |   ✗   |  ✓(比例) |   ✗  |  O(M) |   3.54   |
| h3_tiered (改进版)    |   ✓  |   ✗   |  ✓(分层) |   ✗  |  O(M) | **2.08** |
| OnTPC简化            |   ✓  | **✓** | ✓(L_k) |   ✗  |  O(M) |  预计 1-2  |
| Price-Q95 Ratio    |   ✓  | ✓(软,双信号) |  ✓(软)  |   ✗  |  O(M) |  预计 1-2  |
| Threshold Cheapest |   ✓  |   ✗   |  ✓(阈值) |   ✗  |  O(M) |  预计 2-3  |
| Adaptive Two-Tier  |   ✓  |   轻微  | ✓(自适应) |   ✗  |  O(M) |   预计 1   |
| DA-Tiered          |   ✓  |   ✗   | ✓(阶梯) |   ✗  |  O(M) |     —     |
| DA-Nonlinear       |   ✓  |   ✗   | ✓(非线性) |   ✗  |  O(M) |     —     |

