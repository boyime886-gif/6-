# 启发式调度策略

## 一、实验设计

实验设置：25 个实验，5 种容量配置 × 5 种价格配置，20 轮仿真。

**容量配置**：

| 类型                 | 容量 [S1→S5]            |  梯度比 |
| ------------------ | --------------------- | :--: |
| 单台服务器就能满足要求        | `90k,90k,90k,90k,90k` | 1.00 |
| 3台满足要求·均匀          | `32k,32k,32k,32k,32k` | 1.00 |
| 3台满足要求·梯度变化        | `32k,32k,33k,35k,38k` | 1.20 |
| 5台服务器容量之和满足要求·均匀   | `19k,19k,19k,19k,19k` | 1.00 |
| 5台服务器容量之和满足要求·梯度变化 | `12k,17k,20k,22k,24k` | 2.00 |

峰值需求约 90k。容量充裕时 1-3 台即可覆盖；紧张时需 5 台全量。

**价格配置**：每种容量配置分别与 5 种价格组合，共 25 个实验。实验编号按价格分组：1-5 无价差，

|  实验编号 |    价格组    | 价格 [S1→S5]                 |
| :---: | :-------: | -------------------------- |
|  1-5  |    无价差    | `1.00,1.00,1.00,1.00,1.00` |
|  6-10 | step 0.01 | `1.00,1.01,1.02,1.03,1.04` |
| 11-15 | step 0.02 | `1.00,1.02,1.04,1.06,1.08` |
| 16-20 | step 0.05 | `1.00,1.05,1.10,1.15,1.20` |
| 21-25 | step 0.10 | `1.00,1.10,1.20,1.30,1.40` |

每组内 5 个实验按容量排列：90k×5 → 32k×5 → 32k→38k → 19k×5 → 12k→24k。

***

## 二、五种策略

### 2.1 Cheapest（最便宜优先）

**规则**：永远优先满足单价最低的服务器，当服务器价格一致时随机选择。

**复杂度**：O(M)。

***

### 2.2 Load Balance（负载均衡）

**规则**：选当前时间片绝对负载最小的服务器，负载一致时随机。

**适用场景**：无价差且容量均匀时最优。

**复杂度**：O(M)。

***

### 2.3 Weighted Load Balance（价格加权负载均衡）

**核心思想**：cheapest 只看价格，load_balance 只看负载。把两者揉成一个权重——`price × demand`——在单个时间片内按加权负载选服务器，每时间片重置。

**规则**：

```
初始化: weighted_load(c) = 0   %每台服务器初始的加权负载值为0

每个请求 d_k 到达:
  for each candidate d_k :
      选 weighted_load(c) 最小的   

  分配后: weighted_load(c) += price(c) × d_k
```

***

### 2.4 Dsplit（需求分类分配）

**核心思想**：同时考虑数据大小与服务器负载与服务器价格。大请求对成本和容量冲击大，需要精细处理；小请求去哪影响都微乎其微。

**规则**：

```
阈值: x=1000

每个请求 d_k 到达:
  if d_k ≤ x (小请求):
      → 轮转依次分配给每台服务器（等价于 load_balance）

  else (大请求):
      → 选 weighted_load(s) 最小的服务器
      → weighted_load(c) += price(c) × d_k
      → 每时间片重置
```

**直觉**：小请求去哪影响微乎其微，轮转撒满。大请求用 `price × demand` 加权——贵服务器接大请求后 weight 涨得快，自然排挤出去，便宜服务器持续接大单直到被填满。

***

### 2.5 Full-Factor（全要素评分）

**核心思想**：价格、需求大小、容量、当前负载四种信号一次性编入一条公式。分母 `rem × free` 同时编码绝对剩余和相对占用率——同一剩余量但更满的服务器惩罚更重。

**规则**：

```
每个请求 d_k 到达:
  ① 预过滤已满服务器
  ② for each candidate d_k:
         rem  = capacity(c) - H(c, t)          %服务器的剩余容量
         free = 1 - H(c,t) / capacity(c)       %剩余容量的比例
         score(s) = price(s) × d_k / max(1, rem × free)  
     选 score 最小的
```

***

### 2.6 Quota-Burst（容量分区 + 免费额度调度）

**核心思想**：每台服务器有 432 个免费时间片。用容量分区界定"冲顶线"，然后主动调度每台服务器的免费额度——在需要加班时选最便宜的还有额度的服务器，全力灌满。

**阶段 1 — 容量分区**：

```
总保守预算 = 62500
P(s) = 62500 × cap(s) / Σcap    ← 保守容量（负载在此以下不触发冲顶）
L(s) = cap(s) - P(s)            ← 激进容量（超过 P(s) 的部分可能免费）
```

**阶段 2 — 在线分配**：

```
═══ 30 天周期开始 ═══
  burst_quota(s) = 432    ← 每台服务器 432 个免费时间片额度

═══ 每个时间片 t ═══
  if 所有服务器 H(s,t) ≤ P(s):
      → load_ratio（选 H(s,t)/capacity(s) 最小的，即负载率均衡）

  else:                              ← 有人超过保守线
      candidates = burst_quota > 0 的服务器
      if candidates 为空:            ← 所有服务器额度耗尽
          burst_quota(s) = 432 (全部刷新)
          candidates = 所有服务器
      → cheapest(candidates)         ← 在有额度的服务器中选最便宜的
      → 当前片全力灌满选中服务器

═══ 时间片 t 结束后 ═══
  if H(s,t) > P(s):  burst_quota(s) -= 1
```

**保证**：每台服务器最多 432 个时间片成为 cheapest 目标。432×5=2160 个时间片被明确调度（谁的便宜先用谁），其余 6480 片走负载率均衡。免费槽自动错峰——便宜的先上，用完额度自动轮换到下一台。

***

###

